[package]
name = "cortex-python"
version = "2.0.0"
edition = "2021"

[lib]
name = "cortex_ai_memory"
crate-type = ["cdylib"]

[dependencies]
cortex-core = { path = "../cortex-core" }
pyo3 = { version = "0.23", features = ["extension-module"] }
uuid = { version = "1", features = ["v4"] }
